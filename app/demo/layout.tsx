import type {Metadata} from "next";
import type {ReactNode} from "react";
export const metadata:Metadata={title:"Business Demo | SolProvo",description:"Evidence-first business website preview generated from public business context."};
export default function DemoLayout({children}:{children:ReactNode}){return children}
